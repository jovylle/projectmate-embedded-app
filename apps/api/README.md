# @projectmate/api

Reserved for:

- `POST /feedback` (or similar) to accept submissions from the overlay when `feedbackEndpoint` points here
- Server-side GitHub token usage for releases/issues (Phase 2)
- OpenAI or other model proxy for chat (Phase 2)

Phase 1 uses a bring-your-own `feedbackEndpoint` URL or no server.
